import SwiftUI

struct AppColors {
    static let buttonPrimary = Color.blue
    static let buttonSecondary = Color.green
    static let buttonDisabled = Color.gray
    static let showSavedPDFs = Color.orange
    static let textPrimary = Color.white
    static let textSecondary = Color.gray
}

struct AppGeometry {
    static let cornerRadius = 10.0
}
